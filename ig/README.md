# Usage

These are the scripts to generate the igfiles. There are two types of `.txt` files as input. One list the files needed and the other the `Run:Event` combinations.

The scripts `make_run_event_*lepton.py` only need to be run once to generate the `Run:Event` files.